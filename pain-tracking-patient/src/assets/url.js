const pageUrls = {
	Dashboard: "/dashboard",
	Tracker: "/tracker",
};

const settingUrls = {
	Account: "/account",
	Logout: "/logout",
};

const pages = ["Dashboard", "Tracker"];
const settings = ["Account", "Logout"];

export { pageUrls, settingUrls, pages, settings };
